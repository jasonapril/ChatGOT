# conf/hydra/hydra_logging/custom.yaml
# Define only the custom formatters used by Hydra

formatters:
  minimal:
    format: '%(message)s'
  standard:
    format: '%(asctime)s [%(levelname)s] %(name)s: %(message)s' 